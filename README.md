# DIP-Final
### I. Install requirements

```shell
$ ./install-requirement.sh
```

### II. How to test it

- 首先将包含测试图片信息的 annotation.txt 放到 `code`文件夹中。自行测试的时候可以使用 `generate_test.py` 自行生成测试文件。

```Shell
$ python3 evaluate.py
```

